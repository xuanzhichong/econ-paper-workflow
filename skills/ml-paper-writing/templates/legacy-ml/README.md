# Legacy ML Template Registry

These assets are retained for explicit ML/AI conference use only.

The public package does not ship conference vendor files. This note is retained only to explain that legacy ML templates were intentionally excluded from the shared repository.

If you need ML/AI venue templates, add them separately in your own local setup. They are not part of the default empirical economics workflow.
